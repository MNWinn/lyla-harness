{
  "name": "lyla-agent",
  "version": "0.1.0",
  "description": "A small, model-agnostic agent harness with inspectable local sessions.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22" },
  "bin": { "lyla": "./src/cli.js" },
  "exports": "./src/index.js",
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test",
    "check": "node --check src/cli.js && node --test"
  }
}
